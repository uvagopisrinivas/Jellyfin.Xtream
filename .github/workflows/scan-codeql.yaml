name: "🔬 Run CodeQL"

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/scan-codeql.yaml@master
    permissions:
      security-events: write
      contents: read
      actions: read
    with:
      dotnet-version: "9.0.*"
      repository-name: uvagopisrinivas/Jellyfin.Xtream
